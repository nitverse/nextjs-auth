import LoginForm from "@/components/ui/auth/LoginForm";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <LoginForm />
  )
};

export default page;
